# OS
.DS_Store
Thumbs.db

# Environment / secrets (keep .env.example if you add one)
.env
.env.local
.env.*.local

# --- Frontend (Vite / React / Node) ---
bookstore-frontend/node_modules/
bookstore-frontend/dist/
bookstore-frontend/.vite/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Backend (.NET / ASP.NET Core) ---
BookstoreApi/bin/
BookstoreApi/obj/
BookstoreApi/out/
*.user
*.userosscache
*.suo
*.cache
project.lock.json
project.fragment.lock.json
artifacts/

# Visual Studio / VS Code / Rider
.vs/
.idea/
*.DotSettings.user

# Test / coverage
TestResults/
[Bb]uild/
[Ll]og/
[Ll]ogs/
coverage/
*.coverage
*.coveragexml

# SQLite journal files (safe to ignore even when DB is tracked)
*.sqlite-wal
*.sqlite-shm

# Loose duplicate DB at repo root (use BookstoreApi/Bookstore.sqlite for the app)
/books.db
